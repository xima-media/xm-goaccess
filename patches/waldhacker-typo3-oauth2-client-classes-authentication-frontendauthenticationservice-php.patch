--- /dev/null
+++ ../Classes/Authentication/FrontendAuthenticationService.php
@@ -219,4 +219,12 @@
         }
         return $request;
     }
+
+    public function processLoginData(array &$loginData, $passwordTransmissionStrategy)
+    {
+        $loginData['uname'] = $loginData['uname'] ?? '';
+        $loginData['uident'] = $loginData['uident'] ?? '';
+
+        return true;
+    }
 }
